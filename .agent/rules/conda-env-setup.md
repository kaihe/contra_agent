---
trigger: always_on
---

# Python Environment Rules
ALWAYS activate the specific conda environment before running any Python scripts or pip commands.

- Conda Environment Name: [vllm-env]
- Path: [/home/kaihe/miniconda3/envs/vllm-env/bin/python]

Do NOT use the 'base' environment.
If the terminal does not show (vllm-env), run `conda activate vllm-env` first.